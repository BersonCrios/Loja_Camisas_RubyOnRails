        <tr>
            <td><%= produto.nome %></td> 
            <td><%= produto.descriacao %></td> 
            <td><%= produto.departamento %></td> 
            <td><%= produto.preco %></td> 
            <td><%= button_to "Remover", produto, method: :delete, class: "btn btn-danger", data: {confirm: "Tem certeza que deseja remover #{produto.nome}?"}%></td>
            <td><%= button_to "Alterar", edit_produto_path(produto), method: :get, class: "btn btn-primary"%></td>
        </tr>